interface:
  display_name: "分类美妆素材"
  short_description: "分类美妆/风格妆并加密交付"
  default_prompt: "Use $classify-beauty-assets to classify beauty or style-makeup assets, then encrypt the categorized delivery folders."

policy:
  allow_implicit_invocation: true
